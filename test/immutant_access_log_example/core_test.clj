(ns immutant-access-log-example.core-test
  (:require [clojure.test :refer :all]
            [immutant-access-log-example.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
